import 'persona.dart';

class Repositor extends Persona {
  Repositor({
    legajo,
    nombre,
    puesto,
  }) : super(
          legajo: legajo,
          nombre: nombre,
          puesto: puesto,
        );
}
